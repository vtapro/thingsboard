#
# SPDX-FileCopyrightText: Copyright The Thingsboard Authors
# SPDX-License-Identifier: Apache-2.0
#

# Observability stack for the production cluster. Not part of deploy/k3s/kustomization.yaml:
# it is optional infrastructure and lives in its own namespace.
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    app.kubernetes.io/part-of: thingsboard
